// const express = require("express");
// const projectRoutes = require("../routes/projectRoutes"); // Correct path for your routes

// const app = express(); // Initialize the app

// // Middleware to parse JSON request bodies
// app.use(express.json());

// // Use your routes
// app.use("/api/projects", projectRoutes);


// // Test route
// app.get("/", (req, res) => {
//   res.send("API is running...");
// });

// module.exports = app; // Export the app for server.js or other entry points
